/** Host loader entry for the MCP section browser implementation exported from `./client`. */
/** Host plugin body — no host-side behavior for the MCP settings section. */
function apply() {}
export { apply };
